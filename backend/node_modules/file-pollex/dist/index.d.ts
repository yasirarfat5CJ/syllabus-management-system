import type { Disposer, Event, Handler, Options } from './types';
declare const filePollex: (filePath: string, handler: Handler, options?: Options) => Disposer;
export default filePollex;
export type { Event, Handler, Options };
